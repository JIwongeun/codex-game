# AGENTS.md

## 프로젝트 임무

이 저장소는 OpenAI Game Builders Seoul 2026 Track 1 제출작을 위한 브라우저 게임이다.

- 작업명: `await CODEX: CONTEXT//OVERFLOW`
- 목표: Codex 작업을 기다리는 동안 마우스 포인터로 공격을 피하며 최고 생존 기록에 도전하는 싱글플레이 아케이드
- 핵심 루프: 포인터 이동 → 공격 궤적 판독 → 회피 → 난이도 단계 상승 → 피격 시 즉시 재도전
- 소셜 확장 기능: 시간이 남을 때만 비동기 글로벌 랭킹
- 제출 목표일: 2026-08-25 밤. 공식 접수 종료일인 2026-08-26은 장애 대응 버퍼로 남긴다.

## 작업 시작 전 읽을 문서

다음 순서로 현재 상태를 확인한다.

1. `docs/PROJECT.md` — 제품 범위와 게임 규칙
2. `docs/TECHNICAL.md` — 아키텍처와 서버 경계
3. `docs/DELIVERY.md` — 일정, 완료 조건, 제출 체크리스트
4. `docs/DECISIONS.md` — 확정된 결정과 변경 이유
5. `docs/WORKLOG.md` — 직전 작업, 검증 결과, 다음 행동

문서와 코드가 충돌하면 실제 동작과 테스트 결과를 먼저 확인하고, 같은 작업에서 문서를 현재 상태로 고친다. 제품 방향을 임의로 바꾸지 않는다.

## 우선순위

항상 아래 순서로 판단한다.

1. 브라우저에서 바로 실행되고 한 판을 끝낼 수 있는가
2. 핵심 루프가 이해하기 쉽고 다시 하고 싶은가
3. 크래시, 입력 끊김, 프레임 의존 동작이 없는가
4. 타격감과 시각 피드백이 명확한가
5. 배포, QA, 썸네일 등 필수 제출 준비가 끝났는가
6. 시간이 남으면 글로벌 랭킹이 필요한가
7. 추가 콘텐츠가 필요한가

새 기능 때문에 위 항목이 위험해지면 새 기능을 제외한다.

## 고정 제품 제약

- 브라우저에서 설치, 로그인, 별도 승인 없이 플레이할 수 있어야 한다.
- 첫 방문자는 로그인, 이름 입력, 프로필 생성 없이 익명으로 바로 시작할 수 있어야 한다.
- 글로벌 랭킹 작업을 실제로 시작하기 전에는 게스트 ID나 표시명 시스템을 만들지 않는다.
- 데스크톱 브라우저와 마우스 입력을 우선한다. 키보드 이동과 Space 시작·재시작도 지원한다.
- 시간 제한과 목숨은 없다. 한 번 피격되면 종료되며 생존 시간이 기록이다.
- 첫 조작까지 한 번의 클릭만 요구한다.
- 게임은 랭킹 서버가 없어도 끝까지 플레이되어야 한다.
- 글로벌 랭킹을 구현한다면 비동기 점수 공유만 사용하며 WebSocket 실시간 멀티플레이를 만들지 않는다.
- 런타임 OpenAI API 호출은 핵심 플레이에 포함하지 않는다.
- 브라우저에서 사용자의 실제 설치 앱, 파일, 탭 목록을 읽지 않는다.
- 제3자 앱 로고, 상표, 캐릭터를 복제하지 않는다. 적과 UI는 가상의 개발 도구·프로세스로 만든다.
- 발표 문구와 피치 작성은 사용자가 요청할 때만 다룬다.

## 기술 기준

- 패키지 관리자: `pnpm`
- 언어와 빌드: TypeScript + Vite
- 게임 런타임: Phaser 3
- UI 프레임워크: 도입하지 않는다. 게임 캔버스와 최소 DOM만 사용한다.
- 논리 해상도: 1280×720, 반응형 `FIT` 스케일
- 게임 규칙과 점수 계산은 가능하면 Phaser Scene과 분리한 순수 함수로 작성한다.
- 시간 기반 동작은 프레임 수가 아니라 delta time을 사용한다.
- 새로운 production dependency는 핵심 구현에 필요한 경우에만 추가하고 `docs/DECISIONS.md`에 이유를 남긴다.

## 범위 제한

MVP 이전에는 다음을 구현하지 않는다.

- 실시간 멀티플레이, 채팅, 친구 기능
- 사용자 계정, 이메일, 소셜 로그인
- 여러 맵, 스킨 상점, 업적, 스토리 모드
- 복잡한 장비·스킬 트리
- 완전한 치트 방지 또는 서버 권위형 게임 시뮬레이션
- 실제 Codex 작업 상태와의 연동

## 작업 방식

- 한 번에 가장 작은 플레이 가능한 변경을 만든다.
- 기존 스타일을 따르고 요청과 무관한 리팩터링을 하지 않는다.
- 제품 결정이 달라지면 먼저 `docs/DECISIONS.md`에 새 결정과 대체된 결정을 기록한다.
- 의미 있는 작업을 끝내면 `docs/WORKLOG.md`에 날짜, 변경, 검증, 남은 위험, 다음 행동을 추가한다.
- Codex 활용 근거가 될 만한 설계·구현·문제 해결은 WORKLOG에 사실대로 남긴다.
- 비밀키는 커밋하지 않는다. 공개 가능한 변수명만 `.env.example`에 둔다.
- Git commit, push, 배포, 외부 서비스 생성은 사용자가 요청하거나 승인한 범위에서만 수행한다.

## 필수 검증

코드 변경 후 가능한 범위에서 다음을 실행한다.

```bash
pnpm check
```

`pnpm check`는 typecheck, unit test, production build를 모두 통과해야 한다. 게임 플레이가 바뀐 경우 브라우저에서 최소한 다음도 직접 확인한다.

- 시작 화면에서 게임 진입
- 마우스와 키보드 입력
- 피격으로 한 판 종료
- 개발 전용 `?qaElapsedSeconds=45`에서 후반 공격 단계
- 재시작
- 랭킹을 구현한 경우 API 실패 시에도 게임 진행과 로컬 최고점 표시

검증하지 못한 항목은 완료했다고 표현하지 말고 WORKLOG에 남긴다.
