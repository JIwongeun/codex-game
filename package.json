{
  "name": "await-codex-context-overflow",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify:production": "node scripts/verify-production.mjs",
    "check": "pnpm typecheck && pnpm test && pnpm build && pnpm verify:production"
  },
  "dependencies": {
    "phaser": "3.90.0",
    "pretendard": "1.3.9"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.53.1",
    "typescript": "5.9.3",
    "vite": "8.2.2",
    "vitest": "4.1.11",
    "wrangler": "4.125.0"
  }
}
